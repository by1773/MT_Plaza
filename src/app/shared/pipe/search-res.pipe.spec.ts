import { SearchResPipe } from './search-res.pipe';

describe('SearchResPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchResPipe();
    expect(pipe).toBeTruthy();
  });
});
