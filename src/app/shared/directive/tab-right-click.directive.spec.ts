import { TabRightClickDirective } from './tab-right-click.directive';

describe('TabRightClickDirective', () => {
  it('should create an instance', () => {
    const directive = new TabRightClickDirective();
    expect(directive).toBeTruthy();
  });
});
