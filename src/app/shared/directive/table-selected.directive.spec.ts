import { TableSelectedDirective } from './table-selected.directive';

describe('TableSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new TableSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
