import { TabMoveDirective } from './tab-move.directive';

describe('TabMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new TabMoveDirective();
    expect(directive).toBeTruthy();
  });
});
